server:
  port: 80

logging:
  level:
    org.hibernate: info

jasypt:
  encryptor:
  bean: jasyptStringEncryptor

spring:
  datasource:
    sql-script-encoding: UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rokroot.asuscomm.com:930/test_db?zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Seoul
    username: ENC(uWTB0Y3ZCKt/8a+pageC2w==)
    password: ENC(M+xfl8bkMd2vjR4Ef8SwppL3gozoLArW)

  jpa:
    generate-ddl: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
